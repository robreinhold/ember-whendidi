<div class="container">
    <div class="menu">
      {{#link-to 'index'}}
          <h1 class="left">
              <em>WhenDidI</em>
          </h1>
      {{/link-to}}
        <div class="left links">
          {{#link-to 'list-tasks'}}
              List of Tasks
          {{/link-to}}
        </div>
    </div>
    <div class="body">
      {{outlet}}
    </div>
</div>